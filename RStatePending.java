public class RStatePending implements RState {
    @Override
    public void process(Reservation reservation) {
        // Implement the process method for the Pending state
    }

    @Override
    public String getStatus(Reservation reservation) {
        // Implement the getStatus method for the Pending state
        return "Pending";
    }
}